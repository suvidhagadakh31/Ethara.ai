{
  "name": "team-task-manager",
  "version": "3.0.0",
  "description": "Production-ready Team Task Manager - Full Stack MERN Application",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev:server": "cd server && node index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "postinstall": "cd server && npm install && cd ../client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["task-manager", "project-management", "mern-stack", "react", "express", "mongodb", "railway"],
  "author": "Team Task Manager",
  "license": "MIT"
}
